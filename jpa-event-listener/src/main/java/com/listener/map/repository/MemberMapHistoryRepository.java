package com.listener.map.repository;

import com.listener.map.entity.MemberMapHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberMapHistoryRepository extends JpaRepository<MemberMapHistory, Long> {
}
